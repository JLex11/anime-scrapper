---
title: Lista de animes
category: Animes
order: 10
description: Obtén el catálogo completo con paginación opcional.
endpoint: /animes
method: GET
playground: true
queryParams:
  - name: page
    description: Numero de pagina del feed directory (1 por defecto).
    example: 2
  - name: pageSize
    description: Cantidad de animes por pagina (por defecto 24).
    example: 24
responseExample:
  - animeId: solo-leveling
    title: Solo Leveling
    type: TV
    rank: 1
    otherTitles:
      - Na Honjaman Rebeleop
    description: Un cazador de rango E obtiene una habilidad que cambia su destino.
    originalLink: https://www3.animeflv.net/anime/solo-leveling
    status: En emision
    genres:
      - Accion
      - Fantasia
    images:
      coverImage: https://tu-dominio/api/image/ZXhhbXBsZS1jb3Zlci10b2tlbg
      carouselImages:
        - link: https://tu-dominio/api/image/ZXhhbXBsZS1jYXJvdXNlbC10b2tlbg
          position: 50% 50%
          width: 1280
          height: 720
    relatedAnimes:
      - animeId: solo-leveling-2
        title: Solo Leveling 2nd Season
        relation: Secuela
    created_at: 2026-03-24T15:20:01.000Z
    updated_at: 2026-03-24T15:20:01.000Z
---
## Descripción

Devuelve un arreglo con la informacion resumida del feed `directory` precalculado. La pagina inicial es `1`.

## Parámetros de consulta

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| `page` | number | Página solicitada (por defecto `1`). |
| `pageSize` | number | Tamano de pagina (por defecto `24`). |

## Respuesta

Cada elemento contiene:

| Campo | Tipo | Descripción |
| --- | --- | --- |
| `animeId` | string | Identificador único del anime. |
| `title` | string | Título principal. |
| `type` | string \| null | Tipo de anime (TV, OVA, Película, etc.). |
| `rank` | number \| null | Ranking numérico si está disponible. |
| `otherTitles` | string[] \| null | Títulos alternativos. |
| `description` | string \| null | Sinopsis. |
| `originalLink` | string \| null | URL original en el proveedor. |
| `status` | string \| null | Estado de emisión. |
| `genres` | string[] \| null | Géneros detectados. |
| `images` | object \| null | Imágenes optimizadas para el frontend. |
| `relatedAnimes` | array \| null | Animes relacionados cuando están disponibles. |
| `created_at` | string | Fecha de inserción ISO 8601. |
| `updated_at` | string | Fecha de última actualización ISO 8601. |

## Ejemplo de uso

```bash
curl -X GET "https://tu-dominio/api/animes?page=2&pageSize=24"
```

La respuesta es un arreglo de animes que incluye atributos listos para renderizar tarjetas en el frontend.

## Notas

- Los campos opcionales pueden llegar como `null` si el proveedor no publica esa información.
- `images.coverImage` y `images.carouselImages[].link` apuntan a `/api/image/:token` (redirect 302 firmado).
- El endpoint no dispara scraping: si el feed no existe en DB, responde `404`.
